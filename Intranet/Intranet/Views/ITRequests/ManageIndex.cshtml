@model IEnumerable<Intranet.ViewModels.ITRequestsViewModel>

@{
    ViewBag.Title = "Manage IT Requests";
}

<h2>Manage IT Requests</h2>
@if (TempData["Message"] != null)
{
    <div class="alert alert-dismissible alert-success">@TempData["Message"]</div>
}
<br />

@*@if (User.Identity.Name == "SCGCORP\\Chuck")
{
<p>Assign support requests to staff after clicking the Manage button next to each.</p>
}
else
{
<p>These are the requests assigned to you.</p>
}
<br />*@

<table width="100%">
    <tr>
        <td>
            <div id="demo">
                <table id="datatablesRequests" class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>@Html.DisplayNameFor(model => model.ITRequestID)</th>
                            <th>@Html.DisplayNameFor(model => model.CreatedDate)</th>
                            <th>@Html.DisplayNameFor(model => model.EmployeeName)</th>
                            <th>@Html.DisplayNameFor(model => model.ProjectCode)</th>
                            <th>@Html.DisplayNameFor(model => model.AssigneeName)</th>
                            <th>@Html.DisplayNameFor(model => model.ITRequestStatus)</th>
                            <th>@Html.DisplayNameFor(model => model.StatusDate)</th>
                            <th>@Html.DisplayNameFor(model => model.ITRequestType)</th>
                            <th></th>
                        </tr>
                    </thead>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.ITRequestID</td>
                            <td>@item.CreatedDate.ToShortDateString()</td>
                            <td>@item.EmployeeName</td>
                            <td>@item.ProjectCode</td>
                            <td>@item.AssigneeName</td>
                            <td>@item.ITRequestStatus</td>
                            <td>@item.StatusDate.ToShortDateString()</td>
                            <td>@item.ITRequestType</td>
                            <td>
                                @Html.ActionLink("View", "Details", new { id = item.ITRequestID, Manage = "Yes" })
                                <text>|</text>
                                @if (item.ITRequestStatus != "Complete")
                                {
                                    @Html.ActionLink("Manage", "ManageRequest", new { id = item.ITRequestID, Manage = "Yes" })
                                }
                                else
                                {
                                    <span class="text-muted">Manage</span>
                                }
                                <text>|</text>
                                @if (item.ITRequestStatus == "Submitted")
                                {
                                    @Html.ActionLink("Delete", "ManageDelete", new { id = item.ITRequestID })
                                }
                                else
                                {
                                    <span class="text-muted">Delete</span>
                                }
                            </td>

                        </tr>
                    }
                </table>
            </div>
        </td>
    </tr>
</table>

